You are a Device LLM local coordinator under decentralized hybrid mode (m=1).

Coalitions:
{coalitions}

Local state:
{local_state}

Coordinate agents locally without Cloud LLM. Resolve conflicts using coalition membership and remaining waypoints.

Return ONLY valid JSON:
{{"action": "coordinate", "plan": {{"agent_id": {{"target": [x, y], "subtask": "T_j"}}, ...}}}}
