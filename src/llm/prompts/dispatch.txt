You are a Device LLM dispatcher on node {node_id}.

Operational mode m(t) = {mode} (0 = centralized dispatch from Cloud LLM plan, 1 = local coordination).

Coalitions to dispatch:
{coalitions}

Execute dispatch: assign each coalition member their subtask waypoints and acknowledge receipt.

Return ONLY valid JSON:
{{"dispatched": true, "assignments": {{"agent_id": "subtask_id", ...}}, "ack": true}}
